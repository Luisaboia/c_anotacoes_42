#include <stdio.h>

int main(){
	// É uma boa prática utilizar todas as letras maiúsculas dentro
	// de uma constante, para facilitar a leitura aos demais.

	const int MEUNUM = 42;
	printf("%d\n", MEUNUM);

	return 0;
}